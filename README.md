
# منصة حجز الأنشطة الرياضية الآلية 🏊‍♂️⚽

## نظرة عامة على المشروع

منصة شاملة لحجز الأنشطة الرياضية بشكل آلي تتضمن:
- حجز الملاعب الرياضية (كرة قدم، كرة سلة، تنس، كرة طائرة)
- حجز حمامات السباحة (جلسات مفتوحة وخاصة)
- بوت واتساب للعملاء
- بوت تلجرام للإدارة
- نظام دفع وتأكيد الحجوزات

## 🏗️ هيكل المشروع

### 📁 قاعدة البيانات (`src/database/`)
```
database/
├── connection.ts     # اتصال قاعدة البيانات مع MySQL
└── schema.sql        # هيكل الجداول وبيانات تجريبية
```

**الجداول الرئيسية:**
- `clients` - بيانات العملاء
- `swimming_activities` - أنشطة السباحة
- `field_activities` - الملاعب الرياضية
- `pending_bookings` - الحجوزات المعلقة
- `confirmed_bookings` - الحجوزات المؤكدة
- `payments` - المدفوعات والتحويلات

### 📁 أنواع البيانات (`src/types/`)
```
types/
└── database.ts       # واجهات TypeScript لجميع الجداول
```

### 📁 مستودعات البيانات (`src/repositories/`)
```
repositories/
├── ClientRepository.ts     # إدارة بيانات العملاء
├── ActivityRepository.ts   # إدارة الأنشطة الرياضية
├── BookingRepository.ts    # إدارة الحجوزات
└── PaymentRepository.ts    # إدارة المدفوعات
```

### 📁 الخدمات (`src/services/`)
```
services/
├── BookingService.ts   # منطق الحجوزات
├── WhatsAppBot.ts     # بوت واتساب للعملاء
├── TelegramBot.ts     # بوت تلجرام للإدارة
└── BotManager.ts      # مدير النظام العام
```

## 🔧 متطلبات التشغيل

### البرامج المطلوبة:
- **Node.js** (الإصدار 18 أو أحدث)
- **MySQL** (الإصدار 8.0 أو أحدث)
- **npm** أو **yarn**

### المتغيرات البيئية المطلوبة:
قم بإنشاء ملف `.env` في جذر المشروع:

```env
# إعدادات قاعدة البيانات
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=your_username
DB_PASSWORD=your_password
DB_DATABASE=sports_booking

# إعدادات بوت تلجرام
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
TELEGRAM_CHAT_ID=your_admin_chat_id

# إعدادات واتساب (اختيارية للاختبار)
WHATSAPP_TOKEN=your_whatsapp_token
```

## 🚀 خطوات التشغيل

### 1. استنساخ المشروع
```bash
git clone <repository-url>
cd sports-booking-platform
```

### 2. تثبيت الحزم
```bash
npm install
```

### 3. إعداد قاعدة البيانات

#### أ. إنشاء قاعدة البيانات:
```sql
CREATE DATABASE sports_booking;
USE sports_booking;
```

#### ب. تشغيل ملف الهيكل:
```bash
mysql -u your_username -p sports_booking < src/database/schema.sql
```

أو نسخ محتوى `src/database/schema.sql` وتشغيله في MySQL Workbench

### 4. إعداد متغيرات البيئة
```bash
cp .env.example .env
# قم بتعديل الملف بالقيم الصحيحة
```

### 5. تشغيل المشروع
```bash
# للتطوير
npm run dev

# للإنتاج
npm run build
npm start
```

## 🔄 تدفق العمل

### 1. تسجيل العملاء
- العميل يرسل رسالة عبر واتساب
- النظام يتحقق من وجود العميل بالرقم
- إذا لم يكن مسجلاً، يطلب البيانات الكاملة:
  - الاسم الكامل
  - العنوان
  - رقم الهوية الوطنية

### 2. عملية الحجز
```
1. اختيار نوع النشاط (ملاعب / سباحة)
2. اختيار نوع محدد (مثال: ملعب كرة قدم)
3. اختيار الوقت المتاح
4. تأكيد التفاصيل والسعر
5. إنشاء حجز معلق
6. إرسال تعليمات الدفع
```

### 3. نظام الدفع
- العميل يرسل مرجع التحويل
- النظام يحفظ بيانات الدفع
- إشعار المدير عبر تلجرام
- المدير يؤكد أو يرفض الدفع
- إشعار العميل بالنتيجة

## 🤖 أوامر بوت تلجرام للإدارة

### أوامر عامة:
- `/start` - رسالة الترحيب
- `/help` - قائمة الأوامر المتاحة
- `/stats` - إحصائيات الحجوزات

### إدارة الحجوزات:
- `/pending` - عرض الحجوزات المعلقة
- `/confirm [booking_id]` - تأكيد حجز
- `/cancel [booking_id]` - إلغاء حجز

### أمثلة:
```
/stats
/pending
/confirm BK1234567890
/cancel BK1234567890
```

## 📱 تدفق واتساب للعملاء

### مثال على محادثة:
```
عميل: مرحبا
بوت: أهلاً وسهلاً! اختر الخدمة المطلوبة:
     1️⃣ الملاعب الرياضية
     2️⃣ حمام السباحة

عميل: 1
بوت: اختر نوع الملعب:
     1️⃣ ملعب كرة قدم - 200 ريال/ساعة
     2️⃣ ملعب كرة سلة - 150 ريال/ساعة
     3️⃣ ملعب تنس - 100 ريال/ساعة

عميل: 1
بوت: الأوقات المتاحة لملعب كرة القدم:
     08:00 - 10:00 - 12:00 - 14:00
     أكتب الوقت المطلوب:

عميل: 10:00
بوت: ممتاز! تفاصيل الحجز:
     📅 التاريخ: اليوم
     ⏰ الوقت: 10:00
     ⚽ النوع: ملعب كرة قدم
     💰 السعر: 200 ريال
     
     أكتب رقم هاتفك للمتابعة:
```

## 🧪 الاختبار

### اختبار النظام محلياً:
```bash
npm run dev
```

### اختبار البوتات:
1. تشغيل النظام
2. استخدام الأوامر التجريبية في وحدة التحكم
3. مراقبة اللوجات للتأكد من عمل النظام

### اختبار قاعدة البيانات:
```sql
-- التحقق من الجداول
SHOW TABLES;

-- اختبار البيانات التجريبية
SELECT * FROM swimming_activities;
SELECT * FROM field_activities;
```

## 📊 مراقبة النظام

### اللوجات المهمة:
- اتصال قاعدة البيانات
- رسائل البوتات
- عمليات الحجز
- حالات الخطأ

### إحصائيات متاحة:
- إجمالي الحجوزات
- الحجوزات المعلقة/المؤكدة/الملغية
- حجوزات اليوم
- إيرادات اليوم

## 🔒 الأمان

### نصائح مهمة:
- لا تشارك متغيرات البيئة
- استخدم اتصال SSL لقاعدة البيانات
- قم بعمل نسخ احتياطية دورية
- راقب محاولات الدخول غير المصرح بها

## 🐛 استكشاف الأخطاء

### مشاكل شائعة:

#### خطأ في الاتصال بقاعدة البيانات:
```
❌ Database connection failed
```
**الحل:** تأكد من إعدادات قاعدة البيانات في `.env`

#### خطأ في بوت تلجرام:
```
❌ Telegram bot not connected
```
**الحل:** تأكد من صحة `TELEGRAM_BOT_TOKEN` و `TELEGRAM_CHAT_ID`

#### خطأ في الجداول:
```
❌ Table doesn't exist
```
**الحل:** تشغيل `schema.sql` مرة أخرى

## 📞 الدعم التقني

### للمساعدة:
1. راجع هذا الدليل أولاً
2. تحقق من اللوجات في وحدة التحكم
3. تأكد من إعدادات قاعدة البيانات
4. تحقق من متغيرات البيئة

### معلومات مفيدة:
- إصدار Node.js المطلوب: 18+
- إصدار MySQL المطلوب: 8.0+
- المنصات المدعومة: Windows, macOS, Linux

## 🔄 التحديثات المستقبلية

### الميزات المخططة:
- [ ] دعم المدفوعات الإلكترونية
- [ ] تطبيق موبايل
- [ ] تقارير متقدمة
- [ ] إشعارات تلقائية
- [ ] حجز مسبق لفترات طويلة

---

**تم تطوير هذا النظام باستخدام:**
- React + TypeScript
- Node.js + Express
- MySQL
- WhatsApp Business API
- Telegram Bot API

**الإصدار:** 1.0.0  
**تاريخ آخر تحديث:** ديسمبر 2024
